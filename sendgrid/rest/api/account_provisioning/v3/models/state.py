from enum import Enum
from enum import Enum


class State(Enum):
    ACTIVATED = "activated"
    DEACTIVATED = "deactivated"
    SUSPENDED = "suspended"
    BANNED = "banned"
    INDETERMINATE = "indeterminate"
